---
import site from "@data/site.json";
const currentYear = new Date().getFullYear();
---

<footer class="site-footer section-lg" role="contentinfo">
  <div class="container" style="--spread: var(--size-span-lg)">
    <p>
      Copyright &copy; {currentYear}
      <strong>{site.name}</strong>. Most rights reserved.
    </p>
  </div>
</footer>

<style>
  .site-footer {

    /* position: sticky;
    bottom: 0; */
    border-top: var(--border-size-1) solid var(--separator-surface);
    background-color: var(--gloss);
    color: var(--muted);
  }

  .site-footer p {
    font-family: var(--font-mono);
    font-size: var(--font-size-0);
  }

  .site-footer > div {
    padding-left: var(--gutter);
    padding-right: var(--gutter);
  }

  .site-footer > div {
    text-align: right;
  }
</style>
